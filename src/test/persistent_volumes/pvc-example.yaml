kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: consumer-capacity-pvc-3
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: pd-example
  resources:
    requests:
      storage: 1Gi
